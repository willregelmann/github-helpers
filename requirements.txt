# No third-party Python dependencies — these tools use only the standard
# library and shell out to the GitHub CLI (`gh`) for all API access.
# Install `gh` from https://cli.github.com/ and run `gh auth login`.
